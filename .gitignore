.DS_Store
node_modules
*.log

# npm run build:js 的 JavaScript 产物（Docker / Electron 运行时用，不提交）
dist/
# 同一次构建的 ESM 产物，只经由 ./serverless 子路径消费
dist-esm/

.idea/
# package-lock.json

.vscode
coverage/
test-results/
.trae/specs
# QQ 原生扫码登录的 Android device context（含 QIMEI 等装置识别值，不得提交）
.auth-state/

# 本地 AI 工具配置，与项目源码无关，不进仓库
.claude/
.skills/

# §8.4 wrangler dev / vercel dev 人工验收用的一次性 harness，不是 S5 的真实部署配置，不进仓库
.gate-probe/
.vercel/
.wrangler/
